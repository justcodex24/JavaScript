"use strict"; //* because of this statement all the code will be treated as the newer versions of javascript(ES6).

//alert("It will not work in Nodejs!") //* In node js alert statement can't be execute beacuse its an window or document object.

console.log(typeof null)